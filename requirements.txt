flask
flask-cors
flask-jwt-extended
werkzeug
gunicorn
numpy
scipy
pillow
librosa
exifread
transformers
# Use headless OpenCV to avoid "libGL.so not found" errors and save 50MB+
opencv-python-headless
# Install CPU-only Torch to save 2GB+ of download size (Critical for HF Spaces)
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
# DeepFace requires a backend. Since we are using CPU, let's stick to lightweight backends.
deepface
# TensorFlow CPU is safer for Hugging Face than the full GPU version
tensorflow-cpu