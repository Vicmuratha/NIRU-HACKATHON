[pytest]
markers =
    slow: marks tests that require model inference (deselect with '-m "not slow"')

# Default: skip slow tests. Run all with: pytest -m "" or pytest --run-slow
addopts = -m "not slow"
